# WorkspaceAI-Frontend

Simple frontend for WorkspaceAI sign-in with Google and email/password.

## Setup

1. Copy `.env.example` to `.env`
2. Install deps + run:

```bash
npm install
npm run dev
```

## Notes

- UI-only demo (no backend wiring yet).
